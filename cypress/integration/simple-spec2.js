describe('Sample Spec 2', function() {
    it('adds 4+4', function() {
        assert.isTrue(4+4===8);
        assert.isTrue(0 < 1);
        /**
         # Here are extended comments (for testing of Mocha Reports)
         - ones
         - two
        */
    })
})
